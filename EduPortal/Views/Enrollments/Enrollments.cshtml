@model IEnumerable<EduPortal.Models.EnrollmentViewModel>
@{
    ViewData["Title"] = "📜 Записи";
}

<h2 class="mb-4">📜 Записи студентів на курси</h2>

<table class="table table-striped table-hover shadow-sm">
    <thead class="table-dark">
    <tr>
        <th>Студент</th>
        <th>Курс</th>
        <th>Дата запису</th>
        <th>Оцінка</th>
        <th>Дії</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var enrollment in Model)
    {
        <tr>
            <td>@enrollment.StudentName</td>
            <td>@enrollment.CourseTitle</td>
            <td>@enrollment.EnrollmentDate.ToShortDateString()</td>
            <td>@(enrollment.Grade.HasValue ? enrollment.Grade.ToString() : "-")</td>
            <td>
                <a class="btn btn-sm btn-primary" href="/Enrollment/Edit/@enrollment.EnrollmentId">✏️ Редагувати</a>
                <a class="btn btn-sm btn-danger" href="/Enrollment/Delete/@enrollment.EnrollmentId">🗑 Видалити</a>
            </td>
        </tr>
    }
    </tbody>
</table>

<a class="btn btn-success" href="/Enrollment/Create">➕ Додати запис</a>