@model IEnumerable<EduPortal.Models.CourseViewModel>
@{
    ViewData["Title"] = "📚 Курси";
}

<h2 class="mb-4">📚 Список курсів</h2>

<table class="table table-striped table-hover shadow-sm">
    <thead class="table-dark">
    <tr>
        <th>Назва</th>
        <th>Опис</th>
        <th>Категорія</th>
        <th>Викладач</th>
        <th>Студенти</th>
        <th>Дії</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var course in Model)
    {
        <tr>
            <td>@course.Title</td>
            <td>@course.Description</td>
            <td>@course.Category</td>
            <td>@course.TeacherName</td>
            <td>
                <a class="btn btn-sm btn-primary" href="/Course/Edit/@course.CourseId">✏️ Редагувати</a>
                <a class="btn btn-sm btn-danger" href="/Course/Delete/@course.CourseId">🗑 Видалити</a>
            </td>
        </tr>
    }
    </tbody>
</table>

<a class="btn btn-success" href="/Course/Create">➕ Додати курс</a>