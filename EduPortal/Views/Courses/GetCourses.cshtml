@model IEnumerable<EduPortal.Entities.Course>
@{
    ViewData["Title"] = "📚 Курси";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>📚 Список курсів</h2>
    <a href="/Course/create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Додати курс
    </a>
</div>

@if (Model.Any())
{
    <div class="row">
        @foreach (var course in Model)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title text-primary">@course.Title</h5>
                        <p class="card-text text-muted">@course.Description</p>
                        
                        @if (!string.IsNullOrEmpty(course.Category))
                        {
                            <span class="badge bg-secondary mb-2">@course.Category</span>
                        }
                        
                        <div class="mb-3">
                            <small class="text-muted">
                                <strong>Початок:</strong> @course.StartDate.ToString("dd.MM.yyyy")
                            </small>
                            <br>
                            <small class="text-muted">
                                <strong>Кінець:</strong> @course.EndDate.ToString("dd.MM.yyyy")
                            </small>
                        </div>
                        
                        <p class="card-text">
                            <small class="text-muted">
                                <strong>Викладач:</strong> @course.TeacherName
                            </small>
                        </p>
                    </div>
                    
                    <div class="card-footer bg-transparent">
                        <div class="btn-group w-100" role="group">
                            <a href="/Course/@course.CourseId" class="btn btn-outline-primary btn-sm">
                                <i class="bi bi-eye"></i> Переглянути
                            </a>
                            <a href="/Course/edit/@course.CourseId" class="btn btn-outline-warning btn-sm">
                                <i class="bi bi-pencil"></i> Редагувати
                            </a>
                            <a href="/Course/delete/@course.CourseId" class="btn btn-outline-danger btn-sm">
                                <i class="bi bi-trash"></i> Видалити
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <div class="text-center py-5">
        <div class="mb-4">
            <i class="bi bi-book" style="font-size: 4rem; color: #6c757d;"></i>
        </div>
        <h4 class="text-muted">Курси не знайдено</h4>
        <p class="text-muted">Поки що немає жодного курсу. Створіть перший курс!</p>
        <a href="/Course/create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Створити курс
        </a>
    </div>
}
