@model EduPortal.Entities.Course
@{
    ViewData["Title"] = "📚 Видалити курс";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow border-danger">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0">
                    <i class="bi bi-exclamation-triangle"></i> Підтвердження видалення
                </h4>
            </div>
            <div class="card-body">
                <div class="alert alert-warning" role="alert">
                    <h5 class="alert-heading">
                        <i class="bi bi-warning"></i> Увага!
                    </h5>
                    <p>Ви впевнені, що хочете видалити цей курс? Ця дія незворотна.</p>
                </div>
                
                <div class="card bg-light">
                    <div class="card-body">
                        <h5 class="card-title text-primary">@Model.Title</h5>
                        
                        @if (!string.IsNullOrEmpty(Model.Description))
                        {
                            <p class="card-text">@Model.Description</p>
                        }
                        
                        @if (!string.IsNullOrEmpty(Model.Category))
                        {
                            <span class="badge bg-secondary">@Model.Category</span>
                        }
                        
                        <div class="mt-3">
                            <small class="text-muted d-block">
                                <strong>Початок:</strong> @Model.StartDate.ToString("dd.MM.yyyy")
                            </small>
                            <small class="text-muted d-block">
                                <strong>Кінець:</strong> @Model.EndDate.ToString("dd.MM.yyyy")
                            </small>
                            <small class="text-muted d-block">
                                <strong>Викладач:</strong> @Model.TeacherName (ID: @Model.TeacherId)
                            </small>
                        </div>
                    </div>
                </div>
                
                <form asp-action="DeleteConfirmed" asp-route-id="@Model.CourseId" method="post" class="mt-4">
                    <div class="d-flex justify-content-between">
                        <a href="/Course/Get" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Скасувати
                        </a>
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash"></i> Видалити курс
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
